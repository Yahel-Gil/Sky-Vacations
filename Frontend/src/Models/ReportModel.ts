export class ReportModel {
    public vacationId?: number; // Virtual column from SQL
    public destinationName?: string; // Virtual column from SQL (destination name)
    public totalLikes?: number; // Virtual column (count of likes)
}